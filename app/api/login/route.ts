import { NextResponse } from 'next/server';import bcrypt from 'bcryptjs';import { prisma } from '@/lib/prisma';import { createSession } from '@/lib/auth';
export async function POST(req:Request){const fd=await req.formData();const email=String(fd.get('email'));const password=String(fd.get('password'));const u=await prisma.user.findUnique({where:{email}});if(!u||!await bcrypt.compare(password,u.passwordHash))return NextResponse.redirect(new URL('/login?error=1',req.url));await createSession(u.id);return NextResponse.redirect(new URL(u.setupCompleted?'/dashboard':'/setup',req.url));}

import { prisma } from '@/lib/prisma';import { requireUser } from '@/lib/auth';import { addCar, addCarCost, sellCar } from '@/lib/actions';import { Card, PageTitle, SubmitButton } from '@/components/ui';import { DeleteCarButton } from '@/components/delete-car-button';import { rupiah, todayInput } from '@/lib/utils';
export default async function Cars(){const u=await requireUser();const uid=u!.id;const [cars,accounts]=await Promise.all([prisma.car.findMany({where:{userId:uid},include:{costs:true,debts:true},orderBy:{createdAt:'desc'}}),prisma.account.findMany({where:{userId:uid}})]);return <><PageTitle title="Stok Mobil" desc="Modal, biaya, foto URL, profit/rugi, dan jual mobil otomatis masuk rekening."/><div className="grid xl:grid-cols-[1fr_430px] gap-4"><div className="grid md:grid-cols-2 gap-4">{cars.map(c=>{const cost=c.costs.reduce((a,k)=>a+Number(k.amount),0);const modal=Number(c.purchasePrice)+cost;const profit=c.sellPrice?Number(c.sellPrice)-modal:null;return <Card key={c.id}><div className="flex justify-between gap-2"><div><h3 className="text-xl font-black">{c.name}</h3><p className="text-xs text-slate-400">{c.brand} {c.model} • {c.year} • {c.licensePlate}</p></div><span className={c.status==='SOLD'?'text-rose-300':'text-emerald-300'}>{c.status}</span></div><div className="mt-4 grid grid-cols-2 gap-2 text-sm"><div><p className="text-slate-400">Modal</p><b>{rupiah(modal)}</b></div><div><p className="text-slate-400">Est Jual</p><b>{rupiah(c.estimatedSellPrice)}</b></div><div><p className="text-slate-400">Harga Jual</p><b>{rupiah(c.sellPrice)}</b></div><div><p className="text-slate-400">Profit/Rugi</p><b>{profit===null?'-':rupiah(profit)}</b></div></div>{c.debts&&c.debts.length>0&&<div className="mt-3 rounded-xl bg-rose-400/10 p-3">{c.debts.map(d=><div key={d.id} className="flex justify-between text-sm"><span className="text-rose-300">{d.name}</span><span className="font-black">{d.status==='PAID'?<span className="text-emerald-300">Lunas</span>:rupiah(d.remainingAmount)}</span></div>)}</div>}<details className="mt-4"><summary className="cursor-pointer text-emerald-300">Action</summary><div className="mt-3 space-y-3"><form action={addCarCost} className="grid gap-2"><input type="hidden" name="carId" value={c.id}/><select name="accountId">{accounts.map(a=><option key={a.id} value={a.id}>{a.name}</option>)}</select><input name="amount" placeholder="Biaya"/><input name="description" placeholder="Detail biaya"/><input name="date" type="date" defaultValue={todayInput()}/><button className="btn btn-ghost">Tambah Biaya</button></form>{c.status==='AVAILABLE'&&<form action={sellCar} className="grid gap-2"><input type="hidden" name="carId" value={c.id}/><select name="accountId">{accounts.map(a=><option key={a.id} value={a.id}>{a.name}</option>)}</select><input name="sellPrice" placeholder="Harga jual aktual"/><input name="date" type="date" defaultValue={todayInput()}/><button className="btn btn-primary">Jual Mobil</button></form>}<DeleteCarButton carId={c.id} /></div></details></Card>})}</div><Card><h2 className="font-black mb-4">Tambah Mobil</h2><form action={addCar} className="space-y-3"><input name="name" placeholder="Honda CR-V 2.4 AT 2010" required/><div className="grid grid-cols-1 sm:grid-cols-2 gap-2"><input name="brand" placeholder="Merek"/><input name="model" placeholder="Tipe"/><input name="year" placeholder="Tahun"/><input name="color" placeholder="Warna"/><input name="transmission" placeholder="Transmisi"/><input name="licensePlate" placeholder="Nopol"/><input name="frameNumber" placeholder="No rangka"/><input name="engineNumber" placeholder="No mesin"/><input name="mileage" placeholder="KM"/><input name="purchasePrice" placeholder="Harga beli"/><input name="estimatedSellPrice" placeholder="Estimasi jual"/></div><select name="accountId"><option value="">Stok awal / jangan potong saldo</option>{accounts.map(a=><option key={a.id} value={a.id}>{a.name}</option>)}</select><div className="rounded-2xl border border-white/10 p-3 space-y-2"><p className="text-xs font-black text-slate-400 uppercase">Pinjaman untuk mobil ini (opsional)</p><input name="debtName" placeholder="Nama pemberi pinjaman"/><input name="debtAmount" placeholder="Nominal pinjaman"/></div><textarea name="photos" placeholder="URL foto, satu baris satu foto"/><textarea name="notes" placeholder="Catatan"/><SubmitButton/></form></Card></div></>}
